<%= form_with(model: track, local: true) do |form| %>
  <% if track.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(track.errors.count, "error") %> prohibited this track from being saved:</h2>

      <ul>
      <% track.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :trackname %>
    <%= form.text_field :trackname, id: :track_trackname %>
  </div>

  <div class="field">
    <%= form.label :artist_id %>
    <%= form.select :artist_id, @artist_choice %>
  </div>

  <div class="field">
    <%= form.label :album_id %>
    <%= form.select :album_id, @album_choice %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
