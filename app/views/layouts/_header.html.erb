<div class="header-grid">
  <div class="logo"><%=  image_tag("tempLogo2.jpg", align: 'left', size: "160x42" )%><%= link_to "Welcome to chiTunes", home_index_path %></div>
  <div class="about"><%= link_to "About chiTunes", home_about_path %></div>
  <div class="contact"><%= link_to "How to contact us", home_contact_path %></div>
  <div class="contact">Releases link goes here</div>
  <div class="contact">Artists link goes here</div>
  <div class="contact">Link</div>
  <div class="contact">Link</div>
  <div class="contact">link to profile</div>
  <div class="search"><%= form_tag :controller => 'home', :action => 'search' do %>
      <%= text_field_tag 'searchinput', @searchinput, options = {style:"height:100%;border-radius:5px;width:100%;", placeholder:"Enter search terms, press enter tp search........"} %> </p>
    <% end %></div>
  <div class="hello"><td>
    <% if session[:user_id].nil? %>
      <%= link_to "Create Account", new_customer_path %>
    <% else %>
      Hello <%= Customer.find(session[:user_id]).username %> !
    <% end %>
    <td></div>
  <div class="login"><td>
    <% if session[:user_id].nil? %>
      <%= link_to "Login", login_path, :method => :get %>
    <% else %>
      <%= link_to "Logout", logout_path, :method => :delete %>
    <% end %>
    <td></div>
  <div class="feature3"><%= link_to "Reports", report_reportlist_path %></div>
  <div class="feature4">feature item</div>
  <div class="feature4">feature item</div>
  <div class="feature4">feature item</div>
  <div class="feature4">feature item</div>
  <div class="feature4">feature item</div>
  <div class="feature4">feature item</div>
</div>
